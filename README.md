## Adição dos Passo a Passo.

# Etapa 1 da Atividade (Descrever):
 1. Por que o arquivo .env não deve ser versionado? Contém informações sensíveisO .env geralmente guarda dados como:
  Senhas de banco de dados
  Chaves de API
  Tokens de autenticação
  Configurações privadas
 2. Qual a função do pip freeze no contexto de desenvolvimento em equipe? O pip freeze serve para listar todas as dependências do projeto com suas versões exatas.
 3. O que pode acontecer se a SECRET_KEY for exposta?
 4. Qual o papel do .gitignore em projetos colaborativos?
# Etapa 2 da Atividade (realizar):
 1. Criar o projeto Django com nome de eventos
 2. Criar ambiente virtual
 3. Instalar dependências
 4. Criar arquivo .env e adicionar a chave secreta
 5. Configurar o Django para usar .env
 6. Criar .gitignore
# Etapa 3: Entrega em “trabalhos” no teams com o link do github:
• Estrutura do projeto
 1. Arquivo .gitignore
 2. Arquivo .env (NÃO subir no repositório — apenas mostrar print)
 3. Arquivo requirements.txt
 4. Código do settings.py configurado
 5. Estrutura das pastas no github
 6. Respostas da 1 etapa, no readme do github
